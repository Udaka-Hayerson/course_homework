package solid.ocp.conformity;

public class LoanApprovalHandler {
    public void approvePersonalLoan(Validator validator){
        if(validator.isValid()){
            //process of the loan
        }
    }
}
